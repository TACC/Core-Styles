/*
Bootstrap's `.h1`-`.h6` heading classes are the same classes TACC already
supports independent of Bootstrap; reuse them instead of redefining.
*/
@import url("../../../tacc/components/headings--docs.postcss");
